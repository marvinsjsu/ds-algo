function fetch(api) {
  return new Promise((resolve, reject) => {
    // make XHR call
    // if success, call resolve and pass data - resolve(data)
    // if fail, call reject and pass error - reject(error)
  });
}

